# 샘플 도면 AI 검토 프로토타입 구상안 (공개 도면 기반)

> 목적: 설계감리원의 도면검토 중 **기계적·반복적 업무**를 AI로 자동화하는 프로토타입.
> 보안 원칙: **실도면 절대 미사용**, 전 구간 로컬 처리. 공개/생성 샘플 도면으로만 개발·검증.

---

## 1. 목표와 범위

**1단계 프로토타입 (4주, 규칙 기반 중심):**
- 도면목록표 vs 제출 도면 대조 (누락/중복 도면 탐지)
- 치수 누락·이상치 탐지
- 일람표(창호/재료) vs 도면 내 표기 불일치
- 축척(scale) 표기 누락/일관성

**이후 단계 (확장):**
- 법규·기준 적합성 (RAG: 건축법, KDS/KCS, 소방)
- 객체 인식 → 도면 간 정합성
- BIM(IFC) 연계 시 간섭검토 자동화

---

## 2. 입력 데이터 확보 (보안 회피)

| 출처 | 내용 |
| --- | --- |
| 건축도우미 (archi.go.kr) | 건축 행정정보, 공개 도면 사례 |
| 공공데이터포털 (data.go.kr) | 건축 관련 공개 데이터 |
| 세움터 공개 사례 / 교육용 교재 | 교육 목적 도면 |
| 직접 생성 (FreeCAD, reportlab) | **오류 주입 테스트용** 도면 제작 — 정상 도면 + 일부러 오류 넣은 버전 |

- 실도면(회사 기밀) 반입 금지
- 스캔본 + 벡터 PDF **두 유형 모두** 샘플에 포함

---

## 3. 처리 파이프라인 (전부 로컬)

```
PDF 투입
  → 벡터/스캔 판별 (텍스트 레이어 유무)
  → 텍스트 추출 (벡터: PyMuPDF / 스캔: PaddleOCR)
  → 표 추출 (일람표·도면목록표·재료표 감지)
  → 규칙 엔진 (룰 기반 검토 — 핵심)
  → LLM 보조 (지적사항 문구 요약·근거 정리, 선택)
  → 리포트 생성 (HTML/PDF + 원본 크롭)
```

---

## 4. 기술 스택

| 구성 | 도구 | 비고 |
| --- | --- | --- |
| 언어 | Python 3.11 | |
| PDF 파싱 | PyMuPDF(fitz), pdfplumber | 벡터 도면 텍스트/표 |
| OCR | PaddleOCR | 한글 특화, 로컬 |
| 표 감지 | pdfplumber / LayoutParser | 일람표 구조화 |
| LLM (선택) | 로컬 ollama(Qwen 등) | 판단은 규칙엔진, LLM은 문구 생성만 |
| 리포트 | HTML → PDF(weasyprint) | |
| 시연 UI (선택) | Streamlit | 드래그&드롭 PDF → 리포트 |

---

## 5. 규칙 엔진 v1 (구체 규칙 예시)

1. **도면목록표 대조**: 목록표의 도면번호(A-101, S-201 등) vs 실제 도면 페이지 존재 → 누락·중복·번호 오류 탐지
2. **치수 체크**: 치수 텍스트 정규식 추출(`\d{3,4}` 패턴) → 축척 반영 시 비현실적 값(예: 벽 두께 10,000mm) 플래그
3. **일람표 대조**: 창호일람표 기호(W1, D2) vs 평면도/입면도 내 기호 → 누락·불일치
4. **축척 표기**: 도면별 축척(1:100, 1:50) 표기 유무, 상세도 축척 적정성
5. **기준층 일관성**: 기준층·검토 기준 표기 일관성 (감리중간보고서 기준층 등 연동 가능)

---

## 6. 검증 방법 (오류 주입 테스트)

- 정상 샘플 도면 N장 + **오류 주입 버전** (치수 삭제, 일람표 수치 변경, 도면번호 누락, 축척 오표기)
- 지표: 탐지율(Recall), 오탐(Precision), 도면당 처리 시간
- 목표: 1차 자동 탐지 **Recall ≥ 80%**

---

## 7. 보안 설계

- 전 구간 로컬 처리. 클라우드 API 사용 시 도면 비식별화 후 제한적 사용 (기본 금지)
- 샘플 데이터만 사용, 실도면 반입 금지
- 프로토타입 종료 시 데이터 삭제
- 추후 실도면 적용 시: 회사 내부 서버(온프레미스) 전환 검토

---

## 8. 마일스톤 (4주)

| 주차 | 내용 |
| --- | --- |
| 1주 | 샘플 도면 확보(10~20장) + PDF 파싱 파이프라인 (텍스트/표 추출 확인) |
| 2주 | 규칙 엔진 v1 (도면목록 대조, 치수 체크, 일람표 대조) |
| 3주 | 오류 주입 테스트 + 정확도 측정/튜닝 |
| 4주 | 리포트 양식 + Streamlit UI + 시연 준비 |

---

## 9. 산출물 — 지적사항 리포트 양식

| No | 도면번호 | 위치(영역) | 구분 | 심각도 | 상세 내용 | 근거 | 원본 크롭 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | A-101 | 평면도 우측 | 치수 누락 | 중 | 2층 거실 창호 폭 치수 미표기 | 일람표 W3과 대조 | 이미지 |
| 2 | S-201 | 3층 보 배근 | 표기 불일치 | 중 | 배근도와 일람표 철근 규격 상이 | D22 vs D25 | 이미지 |

---

## 10. 확장 방향

1. **법규 RAG**: 건축법·KDS/KCS·소방기준 지식베이스 → 적합성 검토 자동화
2. **객체 인식**: 벽/창호/설비 심볼 인식 → 도면 간 정합성·간섭 예비 검토
3. **BIM 연계**: IFC 투입 시 상용 간섭검토(Navisworks 등)와 연계
4. **감리 데이터 연동**: 검측·감리중간보고서·지적사항 이력과 연결해 학습 데이터로 활용
