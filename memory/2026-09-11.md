# 2026-09-11

## 애드웨어 제거 (크롬 광고창 자동 실행 문제)

유이사님 신고: "크롬창(쿠팡 등)이 저절로 뜬다" → 애드웨어 6종 제거 완료.

### 원인 (증거)
- **Smart Windows Favorite / winfavorites** (서명자 MediaClick Inc.)
  - `C:\Users\orisi\AppData\Roaming\winfavorites\favoriteurl.exe`, `favoritesch.exe`
  - 예약작업 `Smart Windows Favorite`, `Schedule for Favorite Update Check` (로그온 트리거)
  - `HKCU\Software\favoriteurls` → `fv=600` (=600초=10분 주기로 URL 오픈), `pid=12`
  - 설치일 2026-02-23 → 이게 쿠팡 등 쇼핑몰 창을 띄운 주범
- **HTTPS Connect SecureConnection** (서명자 MONSTERJ INC, 표시명 "Secure Connection, Inc.")
  - `C:\ProgramData\secureconnection\`, 서비스 `SecureConnection`, 예약작업 `secureconnections`/`SecureConnectionCheck`
  - `update\favoriteurls_setup.home.exe` 를 배포 → favorites 애드웨어 재설치 경로
- 동반 애드웨어: **MiteNews**(powernhit, JCG), **Temperature Indicator**(weaping, Temperature Indicator Inc.), **WindowsOptimizer**(JCG), **Anchortools/Flint**(C:\SoftBridge, softbridge.co.kr), **SmartBridge**(C:\Program Files (x86)\VP\AD, Enliple)

### 조치 (2026-09-11 11:13~)
- 백업: `adware-cleanup-2026-09-11\` (예약작업 XML 4개, run-key 스냅샷, favoriteurls.reg, cleanup.log)
- 4개 예약작업 삭제, 프로세스 중지, 서비스 `SecureConnection` 삭제
- 6종 폴더/파일 삭제, HKCU Run 5개 + Uninstall 레지스트리 정리
- C:\SoftBridge 안의 `dainBroad`(무관한 VOD 플레이어)는 보존
- 건드리지 않음: UbiViewerWS, CrossEX, AnySign, Veraport, MagicLine, IPinside, SignKorea, VPWalletService(결제), 알약 시리즈
- 잔여: `secureconnectionservice` PID 9084 (SYSTEM, 바이너리 삭제됨) → 재부팅 시 사라짐
- 2차 정리: CloudStore 앱메타데이터 캐시 10개 키(favoriteurls/secureconnection/windowsoptimizer) 삭제, 백업 `cloudstore-backup.reg`

### 메모
- 게이트웨이는 `x1_carbon_g12\orisi`, session 1로 실행 중 → `Start-Process -Verb RunAs` + UAC 클릭으로 관리자 작업 가능 (`tools.elevated`는 webchat에서 불가)
- UAC 프롬프트는 약 2분 후 자동 취소됨
- 재발 방지: 번들 설치 시 "추가 프로그램" 체크박스 주의, ALYac 정기검사
- 의심 잔존(미제거): `CLIPDOWN`(C:\Program Files (x86)\clipdown, JS Future), Chrome 확장 `enable-rightclick.freebusinessapps.net` v3.0.0, `User Agent Switcher/URL sniffer`, `Grabbit`

## 추가 애드웨어 발견·제거: ClipDown (19:50~)

저녁에 "애드웨어 없어졌나?" 질문 → 크롬 방문 기록 검증 결과 **아직 남아 있었음**.
- 09-11 18:02 (11:13 제거 이후)에도 `xn--o39an5bf2p1yd8rjol5a.com`(쇼핑바로가기.com) → `searchalgorithm.co.kr/ad/tab_open.php?...type=3&domain=즐겨찾기&title=쿠팡!` → `api.mjbiz.co.kr` → `link.coupang.com` → 쿠팡 창 자동 실행
- 동일 패턴이 09-09 04:50, 09-10 18:04, 09-11 04:41에도 반복 → 주기적 광고 탭
- 원인: **ClipDown** (JS Future Corp., C:\Program Files (x86)\clipdown) — `ad_shortcutbookmark.xml`에 쿠팡/11번가/G마켓/옥션/알리/아고다/호텔스/테무 가짜 바로가기를 XOR('clip')+base64로 난독화해 심음. `ad_config.xml`에 day_tab=10, use_chrome=1
- 크롬 북마크에 실제로 '쿠팡!' 가짜 북마크 2개 심겨 있었음 (→ 쇼핑바로가기.com)
- `ClipdownService`(cdsrvc.exe) 서비스 + 예약작업 `ClipdownBootStrapper`, `ClipDownScheduler_{EF3FBBA1-...}`

### 조치 (19:57, UAC 승인 후)
- 예약작업 2개 삭제, ClipdownService 삭제, 프로세스 종료, uninst.exe /S, 폴더·레지스트리(Uninstall\CLIPDOWN)·시작메뉴 바로가기 삭제 → 전부 GONE 확인
- 크롬 Bookmarks 파일에서 가짜 북마크 2개 제거 (백업: Bookmarks.backup-20260911-195844.json). 크롬 실행 중이므로 재시작 필요
- `AppData\Roaming\TemperatureIndicator` 잔여(쇼핑몰 ico) 삭제
- 로그: pass3-clipdown.log
- 검증 자동화 예약: 2026-09-12 09:00 재발 확인

## 월간 PC 정기 점검 자동화 등록 (13:35~13:44)
- 요청: "한달에 한번 정기 검사해줘" → 범위 확인 결과 **PC 종합 점검 포함**으로 결정
- 크론: 매월 1일 09:00 KST · job id `03457c99-26e4-4510-aad1-6a5565cd5341` · 텔레그램 배달 · isolated agentTurn(thinking low)
- 시험 실행(force) **성공**: 13:44 완료, 110초, 총 36.7만 토큰(input 51.7k / output 5.0k / cache 310.5k), 텔레그램 배달 OK
- **발견 사항**
  - ⚠️ secureconnection 잔존: `HKCU\Software\check_secureconnection`, `HKLM\Software\WOW6432Node\check_secureconnection` (설치일 2026-02-20), `secureconnectionservice.exe` PID 9084 (오늘 04:23 시작, 경로 조회 불가)
  - ⚠️ SmartBridge 레지스트리 흔적만 남음 (HKCU\Software\SmartBridge, SmartBridge_UUID)
  - ⚠️ Chrome 정책 `InsecurePrivateNetworkRequestsAllowed=1` (HKLM Policies)
  - ⚠️ AppCompat shim 의심: `C:\Windows\AppPatch\CustomSDB\{22221111-...}.sdb` (2026-08-28)
  - ✅ favoriteurls/winfavorites/MiteNews/WindowsOptimizer/Anchortools/Flint 흔적 없음 · 예약작업·서비스·설치목록 깨끗
  - PC: C: 951.6GB 중 여유 672.3GB(71%) 양호 · 윈도우 업데이트는 드라이버 5건뿐 · 현장일기 미푸시 커밋 1건(99d1d67)
- 개선 후보: 현장일기 백업 스크립트는 변경 없으면 push를 안 함 → 미푸시 커밋이 남을 수 있음

## 20:16 크롬 종료 후 확인 + 이슈
- 크롬 완전 종료 확인(프로세스 0) → Bookmarks 파일(20:11:58 재기록) 점검: **가짜 쇼핑 북마크 없음** (정상 북마크만: OpenClaw Control, Google, NAVER, 동행복권, YouTube, 인터넷지로, 힛츠, 쿠팡) → 재편집 불필요
- ⚠️ **현장일기 push 실패**: `fatal: Unable to persist credentials with the 'wincredman' credential store`. credential.helper=manager(GCM, 로컬), `~/.git-credentials` 없음 → 읽기(ls-remote)도 동일 실패. 미푸시 커밋 `99d1d67`(9/9) 잔존. `github_identity_status`: agent scope 미설정, native credential unverified → **Settings → Agents → Tools에서 GitHub 재연결 필요** (토큰은 채팅으로 받지 않는다)
- ✅ 9/12 09:00 재발 점검 예약이 **누락돼 있어** 새로 등록 (job id `7dc88b51`, 실행 후 자동 삭제)
- ✅ **pass4 정리 완료 (20:17, UAC 승인)**: `HKCU\Software\check_secureconnection`, `HKLM\SOFTWARE\WOW6432Node\check_secureconnection` 삭제, 크롬 정책 `InsecurePrivateNetworkRequestsAllowed` 제거 → 전부 없음 확인 (admin:True, whoami: orisi). 백업: `pass4-*.reg` 3개, 스크립트: `scripts/pass4-cleanup.ps1`, 로그: `adware-cleanup-2026-09-11\pass4-applied.log`

## 20:23 쿠팡 북마크 정체 극명 (중요)
- **결론: 애드웨어 재발이 아니었다.** 20:11:58에 실행된 `add-bookmark.py`(북마크 주입 검증 테스트)가 만든 것. 근거: 해당 엔트리 `date_added=2026-09-11 11:11:58 UTC(=20:11:58 KST)`, id=19, 신규 guid → 같은 시각 `Bookmarks.before-add-20260911-201158.json` 백업 존재
- **왜 삭제가 안 붙었나**: Chrome Bookmarks 파일의 `checksum`(MD5) 검증. 기존 `clean-bookmarks.py`는 `setdefault('checksum','')`로 **기존 체크섬을 그대로 둠** → 값 불일치 → Chrome이 파일 수정을 무시하고 in-memory 상태로 되돌림 (20:20:09 삭제 → 20:20:46 복원)
- **해결**: `add-bookmark.py`의 `compute_checksum()`을 Chrome 저장 파일과 대조해 MATCH 확인 → `fix-remove-coupang.py`로 제거 + 체크섬 재계산 저장 → 45초 후에도 재발 없음, stored==calc MATCH
- **교훈**: 앞으로 Bookmarks 파일을 직접 수정할 때는 반드시 **체크섬 재계산**. 검증용 북마크는 테스트 후 즉시 제거. 체크섬 알고리즘 참고: `bookmark_codec.cc.txt`(백업 폴더)
- 남은 의심 확장(북마크 생성과는 무관): `iCloud 책갈피`(bookmarks+nativeMessaging, iCloud 미설치 → 고아/가짜 의심), `Chrome™에서 마우스 오른쪽 버튼 클릭`(구 enable-rightclick v3.0.0), `Grabbit`, `User Agent Switcher, URL sniffer`

## 20:20 쿠팡 북마크 제거 + 주말 일정
- 유이사님: "쿠팡 북마크 뜬다" → 크롬 북마크에 남아 있던 `쿠팡 | https://www.coupang.com/` **1건 제거**. ⚠️ 당시엔 애드웨어로 판단했으나, 위 20:23 항목에서 **우리 검증 테스트(add-bookmark.py)가 만든 것**으로 정정됨. 백업: `Bookmarks.backup-removecoupang-20260911-202009.json`, 스크립트: `remove-coupang-bookmark.py`. 크롬 실행 중이라 **완전 종료 후 재시작 필요**
- 유이사님 주말 계획: **금요일 저녁 종료 → 월요일(9/14) 새벽 부팅** → 9/12 09:00 재발 점검을 **9/14 09:00**으로 이동 (job `7dc88b51`). 주말 동안 하트비트·19:00 현장일기 백업은 미실행(정상)

## 20:26 GitHub 백업 연결 진단 (유이사님 질문)
- 유이사님이 제시한 URL `https://github.com/orisispmp-ship-it/site-diary`는 **이미 현장일기 전용 백업 저장소**(루트에 `2026-09-*.md`, `나의일기/`). 워크스페이스 전체를 이 repo로 밀면 파일이 섞이고 히스토리가 달라 push 거부됨 → **워크스페이스용 별도 private repo 필요**
- **워크스페이스 git 상태**: repo는 있었지만 커밋 0개·remote 없음(사실상 백업 없음) → `.gitignore` 생성(`tmp/ state/ .openclaw/ *.key *.pem secrets*` 등 제외) 후 초기 커밋 **`db9209e`** (57파일: AGENTS/SOUL/IDENTITY/USER/DREAMS.md + `memory/` `skills/` `scripts/`). 커밋 전 토큰/키 패턴 스캔 → 검출 없음
- **⚠️ 자격증명 전무**: Windows Credential Manager 비어 있음(`cmdkey /list` = 없음), `gh` 미설치, `GH_TOKEN`/`GITHUB_TOKEN` 없음, OpenClaw secret store 비어 있음 → 이 환경에서 `git push`/`ls-remote` **불가** (GCM 오류: `Unable to persist credentials with the 'wincredman' credential store`, no tty)
- **⚠️ 현장일기 자동 백업은 9/9부터 실제로 실패 중**: 로컬 `main`=`99d1d67`(9/9 현장일기 백업) vs `origin/main`=`a9b8e7d`(9/7 현장일기 백업) → 9/9 커밋이 GitHub에 안 올라감. 매일 19:00 job은 "✅ 완료"/"변경 없음"만 보고(스크립트 `scripts/site-diary-backup.js`가 push 결과와 무관하게 성공 메시지 반환) → **조용한 실패**
- OpenClaw GitHub Identity: 미연결(`configured=false`, native unverified). 문서상 이 신원은 **git credential helper를 설치하지 않으므로** `git push` 인증 문제와는 별개
- GitHub repo 생성은 API 자격증명 없이는 불가 → 사용자가 웹에서 만들거나 PAT/SSH 인증 필요. SSH 키 방식이 비대화형 자동화에 가장 안정적
- 기록 목적: 이 커밋/진단은 워크스페이스 repo `C:\Users\orisi\.openclaw\workspace` 대상 <!-- project: path:C:\Users\orisi\.openclaw\workspace -->

## 20:37 GitHub 백업 연결 완료 (SSH 키 방식)
- **인증**: `~/.ssh/id_ed25519_github`(ed25519, passphrase 없음 — 비대화형 자동 백업용) 생성 + `~/.ssh/config`에 github.com → IdentityFile/IdentitiesOnly 등록. 공개키를 유이사님이 계정 SSH 키로 등록 → `ssh -T git@github.com` = "Hi orisispmp-ship-it!" ✅
- **워크스페이스 백업 연결**: `git@github.com:orisispmp-ship-it/openclaw-workspace.git` (branch main). 첫 push `db9209e` → 이어서 `487b34a`(메모리+스크립트) 푸시. 원격 파일 57개 검증, working tree clean
- **현장일기(site-diary) 복구**: remote를 HTTPS → SSH로 전환(`git@github.com:orisispmp-ship-it/site-diary.git`) 후 9/9부터 밀려 있던 커밋 push (`a9b8e7d..99d1d67`) → `origin/main == main` = `99d1d67` ✅
- **자동 백업 job `1646b637`** 스크립트 교체: push 성공 여부 확인, 실패 시 ❌+오류 메시지, 미푸시 커밋 있으면 ⚠️ 알림 → 강제 실행으로 동작 확인
- **교훈**: 이 환경(Gateway exec, no tty)에서는 GCM/wincredman을 못 씀(`Unable to persist credentials…`) → 헤드리스 자동 백업은 **SSH 키**가 정답. `gh` CLI도 미설치
- 재사용: 향후 다른 저장소 백업도 같은 SSH 키를 쓰면 별도 자격증명 없이 push 가능

## 20:40 워크스페이스 자동 백업 잡 생성 + Push Protection 차단 사건
- 유이사님 요청("매일 19:10에 돌게 해줘") → job `90188a0f-6954-40eb-a240-f44843b6b6cc` **워크스페이스 GitHub 자동 백업 (매일 19:10)** 생성 (cron `10 19 * * *` Asia/Seoul, isolated, 텔레그램 알림). 현장일기 잡(`1646b637`, 19:00)과 동일 패턴(변경 감지·미푸시 감지·push 실패 시 ❌ 보고)
- `.gitignore` 확장: `**/node_modules/`, `**/.edgetmp/`, `uploads/`, `**/__pycache__/`, `**/dist/`, `**/build/`, `**/*.local` — 제외 이유: calendar2027/node_modules 143MB, pdf-compressor/uploads 에 105MB 단일 파일(GitHub 100MB 제한)
- **⚠️ GitHub Push Protection 차단 사건**: 전체 백업 푸시가 `GH013 Repository rule violations` — `감리평가앱/server.mjs:6` 에 **DeepSeek API 키 하드코딩** 감지 → 푸시 거부(비밀은 유출되지 않음, 로컬에만 존재)
  - 조치: 키를 `감리평가앱/deepseek.key.local`(35자, gitignore 대상)로 분리하고 `server.mjs` 6행을 `process.env.DEEPSEEK_API_KEY || readFileSync(new URL('./deepseek.key.local', import.meta.url),'utf-8').trim()` 로 교체 → `node --check` 통과, 잔여 키 0건, amend 후 재푸시 성공(`487b34a..8ae924e`, 원격 242파일)
  - 검증: 원격 트리에 `deepseek.key.local` 없음, `sk-*`·`ghp_*`·`AIza*` 패턴 0건
  - **유이사님 조치 권장**: 평문 파일로 존재했던 키라 DeepSeek 콘솔에서 **키 회전(재발급)** 권장 (GitHub에는 안 올라갔음)
- 교훈: 워크스페이스 자동 백업 전에는 `git grep` 으로 비밀 패턴 **사전 스캔** 필수. Push Protection은 마지막 방어선이므로 로컬 평문 키는 별도 관리
